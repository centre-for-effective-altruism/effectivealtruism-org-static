- var embedSrc = src || href || link || false
- var aspectRatio = aspect || '16by9'
- var embedTitle = title || ''
- var showModalOnLoad = launch
- var embedHeight = height

if embedSrc
	include ../components/responsive-embed
	+embed(embedSrc,aspectRatio,embedTitle,showModalOnLoad,embedHeight)
